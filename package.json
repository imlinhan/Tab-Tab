{
  "name": "tabtab",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "vite build --watch",
    "build": "tsc && vite build",
    "pack": "npm run build && cd dist && zip -r ../tab-tab.zip . --exclude '*.DS_Store' --exclude '*.svg' && cd .."
  },
  "devDependencies": {
    "@types/chrome": "^0.0.287",
    "typescript": "^5.7.3",
    "vite": "^6.3.2"
  }
}
