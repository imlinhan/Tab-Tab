{
  "name": "tabtab",
  "version": "1.0.1",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "vite build --watch",
    "build": "tsc && vite build",
    "build:store": "tsc && vite build --config vite.config.store.ts",
    "pack": "npm run build && rm -f tab-tab.zip && cd dist && zip -r ../tab-tab.zip . --exclude '*.DS_Store' --exclude '*.svg' && cd ..",
    "pack:store": "npm run build:store && rm -f tab-tab-store.zip && cd dist && zip -r ../tab-tab-store.zip . --exclude '*.DS_Store' --exclude '*.svg' --exclude 'manifest.store.json' && cd ..",
    "pack:edge": "npm run build:store && rm -f tab-tab-edge.zip && cd dist && zip -r ../tab-tab-edge.zip . --exclude '*.DS_Store' --exclude '*.svg' --exclude 'manifest.store.json' && cd .."
  },
  "devDependencies": {
    "@types/chrome": "^0.0.287",
    "typescript": "^5.7.3",
    "vite": "^6.3.2"
  },
  "dependencies": {
    "@types/masonry-layout": "^4.2.8",
    "masonry-layout": "^4.2.2"
  }
}
